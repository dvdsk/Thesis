/// newly joining the cluster
/// figure out ips of > 50% of the cluster. If we can not
/// the entire cluster has failed or we have a bad connection.
pub async fn discover_cluster() { 

}

pub async fn maintain_discovery() {}
